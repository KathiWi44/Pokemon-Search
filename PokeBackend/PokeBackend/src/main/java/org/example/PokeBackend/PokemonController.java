package org.example.PokeBackend;

public class PokemonController {

    private final String apiUrl = "https://pokeapi.co/api/v2/pokemon/";
/*
    @GetMapping("/pokemon/{name}")
    public PokemonData getPokemonInfo(@PathVariable String name) {
        String apiUrl = pokeApiUrl + name.toLowerCase();
        RestTemplate restTemplate = new RestTemplate();
        //PokemonInfo pokemonInfo = restTemplate.getForObject(apiUrl, PokemonInfo.class);
        return pokemonInfo;
    }

 */
}
